name: Generate Full Spec Markdown

on:
  workflow_run:
    workflows: ["Generate Full Spec"] # Name deiner ersten Action
    types:
      - completed
  workflow_dispatch: # Erlaubt weiterhin manuellen Start

jobs:
  build-markdown:
    runs-on: ubuntu-latest
    # Nur ausführen, wenn die erste Action erfolgreich war
    if: ${{ github.event.workflow_run.conclusion == 'success' }}
    permissions:
      contents: write

    steps:
      - name: 📥 Checkout Repository
        uses: actions/checkout@v4

      - name: 🐍 Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: 📦 Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyyaml

      - name: ⚙️ Run YAML to Markdown Script
        run: python yaml_to_md.py

      - name: 🚀 Commit and Push full_spec.md
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          
          if [[ -f "full_spec.md" ]] && [[ -n "$(git status --porcelain full_spec.md)" ]]; then
            git add full_spec.md
            git commit -m "docs(spec): auto-generate full_spec.md [skip ci]"
            git push
          else
            echo "Keine Änderungen an der full_spec.md erkannt."
          fi
